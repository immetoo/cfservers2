This directory should hold all merchant specific queries, the queries must
have the file extension .sql in order to be executed by the cron script.

if for any reason queries should be written that cover multiple merchants 
please place these queries in a different directory.