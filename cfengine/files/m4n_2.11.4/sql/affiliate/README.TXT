This directory should hold all affiliate specific queries, the queries must
have the file extension .sql in order to be executed by the cron script.

If for any reason queries should be written that cover multiple affiliates 
please place these queries in a different directory.